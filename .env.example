# Free key from https://etherscan.io/apis — one key covers Ethereum, BSC and Base (Etherscan V2).
ETHERSCAN_KEY=

# Optional. Only needed if you want the app to resolve handles that are NOT in
# data/wallets.json by calling fomo live. Expires ~hourly.
FOMO_TOKEN=
